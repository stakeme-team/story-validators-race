To start using the **SNM** (Story Node Manager) and install snapshot run the command in your terminal:

```
bash <(curl -s https://raw.githubusercontent.com/homelan2er/manager_node/main/story.sh)
```
